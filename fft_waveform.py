import numpy as np
import sys

def fft_wav(waveform, sampling_rate, start_time, goal_time):
    print "fft start"

def dft_wav(waveform, sampling_rate, start_time, goal_time):
    print "dft start"

if __name__ == "__main__":
    argv = sys.argv
    argc = len(argv)

    if argc == 1:
        print "inlavid argument"
        exit()
